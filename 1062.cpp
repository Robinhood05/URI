#include <iostream>
#include <stdio.h>
#include <stdlib.h>
#include <stack>

using namespace std;

int fator(int n)
{
  	for(fat = 1; n > 1; n --)
		fat = fat * n;
	return fat;
}

int main()
{
	int N, chefe[1000];
	
	while (scanf("%d", &N))
	{
		while (true)
		{
			int fat;
			stack <int> T;
			for(int i=0; i < N; i++)
			{
				scanf("%d", &chefe[i])
				
				if (chefe[i] == 0)
				{
					print("\n");
				    break;
				}
			}
			fat = fator(N);
			
			for (int i=0; i < fat; i++)
			{



		
	}
	print("\n");
		
		
	system("pause");
	return 0;
}
