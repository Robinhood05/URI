#include <iostream>
#include <stdio.h>
#include <stdlib.h>
#include <stack>

using namespace std;
int main()
{
	int N;
	
	scanf("%d", &N);
	
	for (int i=0; i < N; i++)
	{
		bool entra = false;
		string eq;
		
		getline(cin, eq);
		
		for (int j=0; j < eq.size(); j++)
		{
			if (entra)
			{
				print ()
			if (eq[j] == '^' )
			{


	system ("pause");
	return 0;
}
